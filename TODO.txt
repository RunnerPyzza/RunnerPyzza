SSH/passwords:
- http://blog.hypergeometric.com/2012/02/22/ssh-dos-and-donts/

RPdeamon:
- When the start command has been issued, the daemon should reply with running instead of queued (it happens only when the first step is not ready)
- When a machine is not reachable the deamon should not crash with error "Host unreachable"
- Lanciando due volte results moure il demone
- Il clean fa morire il demone
- Se chiedo results prima della fine del job il demone deve rispondere fail

Next:
====== Configurazione semplificata
- Si suppone che tutte le macchine che fanno i job siano raggiungibili dal demone via NFS
- Si suppone che tutte le macchine che fanno i job siano raggiungibili con chiave ssh e stesso utente dalla macchina che gira il demone
- Il launcher puo' non mandare le macchine: se le manda il job viene lanciato solo su quelle
	- Gala: sistemare questo aspetto sia sul launcher
	- Emilio: file di configurazione con indirizzi e locazione chiavi SSH
====== Gestione file
- Se l'utente usa NFS deve mettere percorsi assoluti e se non fa son cazzi sua
	- Emilio: prova
- Se l'utente non usa NFS tutti i file di input vengono copiati e il server si crea una directory; quando il server si connette deve lanciare i comandi nella cartelle specifiche
	- Gala: copia dei file / creazione-gestione cartelle / ri-copia dei file

FAIL se facciamo partire una pizza inestistente non se ne accorge il laucher ma solo il demone